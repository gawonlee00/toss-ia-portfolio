# Streamlit Community Cloud 배포

1. GitHub에 새 저장소를 만듭니다.
2. 이 폴더의 파일 전체를 저장소 최상위에 업로드합니다.
3. https://share.streamlit.io/ 에 로그인합니다.
4. Create app / Deploy an app에서 GitHub 저장소를 선택합니다.
5. Main file path는 `app.py`로 지정합니다.
6. 원하는 app URL(subdomain)을 입력하고 Deploy합니다.
7. 배포 후 앱 URL을 열어 실제 동작을 확인합니다.

주의: URL은 Streamlit에서 실제 배포가 완료되어야 확정됩니다.
