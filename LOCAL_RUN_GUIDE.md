# TOSS IA Portfolio Web v1.4.2 로컬 실행

## 일반 실행
`실행.cmd`를 더블클릭합니다.

## 실행되지 않거나 창이 바로 닫히는 경우
`run.cmd`를 더블클릭합니다.

v1.4.2의 `run.cmd`는 진단 종료 후 반드시 `pause`에서 멈추며,
같은 폴더에 `run_log.txt`를 생성합니다.

확인 순서:
1. `run.cmd` 더블클릭
2. 검은 창에 표시되는 마지막 메시지 확인
3. 같은 폴더의 `run_log.txt` 확인
4. 오류가 있으면 `run_log.txt` 내용을 ChatGPT에 붙여넣기

중요:
- 최초 실행 시 패키지를 설치하기 위해 인터넷이 필요할 수 있습니다.
- Python 3가 설치되어 있어야 합니다.
- 앱 자체는 실행 시 `127.0.0.1:8501`에만 바인딩됩니다.
